/**
 * This function represents a page component in a React application.
 * It renders the `Individual` component as the main content of the page.
 *
 * @returns {JSX.Element} - The JSX element representing the page with the `Individual` component.
 */
import Individual from "@/modules/profile/customerprofile/individual";

export default function page() {
  return <Individual />;
}
