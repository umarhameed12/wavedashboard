
import Products from "@/modules/analytics/products";
import React from "react";


/**
 * This is the main function for the page component.
 * It renders the `Products` component.
 *
 * @returns {React.ReactElement} - The rendered `Products` component.
 */
export default function page() {
  return <Products />;
}
